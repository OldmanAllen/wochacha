<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="45dp"
    android:layout_alignParentTop="true"
    android:background="@drawable/title_bar"
    android:padding="10dp" >

    <Button
        android:id="@+id/btn_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:background="@null"
        android:gravity="left|center_vertical"
        android:text="@string/back"
        android:textColor="@color/maintabs_tab_bar_text_color"
        android:textSize="15sp"
        android:textStyle="bold" />

    <CheckBox
        android:id="@+id/cb_nfc_mode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
         android:drawablePadding="5dp"
        android:drawableLeft="@drawable/nfc_btn" android:button="@null"
        android:checked="false" android:text="RFID 查询" android:layout_centerInParent="true"
        android:textColor="@color/maintabs_tab_bar_text_color" />

    <CheckBox
        android:id="@+id/cb_turn_light"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:button="@drawable/light_btn"
        android:checked="false" />

</RelativeLayout>